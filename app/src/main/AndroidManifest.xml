<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ajou.capstone_design_freitag">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".UI.mypage.FinalPaymentActivity"></activity>
        <activity android:name=".UI.mypage.CheckValidatedDataActivity" />
        <activity android:name=".Work.BoundingBoxActivity" />
        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity" />
        <activity android:name=".WebViewActivity" />
        <activity android:name=".Work.TextCollectionActivity" />
        <activity android:name=".UI.mypage.RequestDetailActivity" />
        <activity
            android:name=".PopupActivity"
            android:screenOrientation="portrait"
            android:theme="@style/popupTheme" />
        <activity android:name=".Work.ClassificationActivity" />
        <activity android:name=".Work.AudioCollectionActivity" />
        <activity android:name=".Work.ImageCollectionActivity" />
        <activity android:name=".Work.ProjectDetailActivity" />
        <activity android:name=".LoginActivity" />
        <activity
            android:name=".Audio.AudioTrimmerActivity"
            android:theme="@style/Theme.Transparent" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>